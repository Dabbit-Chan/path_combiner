name: path_combiner
description: "A Flutter package for the animation of the combination of two paths"
version: 0.0.1
homepage: https://github.com/Dabbit-Chan/path_combiner

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
